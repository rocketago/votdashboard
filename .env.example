# Copy to .env and fill in. Never commit the real file.
# Create at https://airtable.com/create/tokens with scopes:
#   schema.bases:read, data.records:read  — granted to your base only
AIRTABLE_TOKEN=
AIRTABLE_BASE=
AIRTABLE_TABLE=Chapters
